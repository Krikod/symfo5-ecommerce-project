{% extends 'base.html.twig' %}

{% block title %}Mon panier{% endblock %}

{% block body %}

    <h1>Mon panier</h1>

    <table class="table">
        <thead>
        <tr>
            <th>Produit</th>
            <th>Prix</th>
            <th>Quantité</th>
            <th>Total</th>
            <th></th>
        </tr>
        </thead>
        <tbody>
            {% for item in items %}
                <tr>
                    <td>{{ item.product.name }}</td>
                    {# |number_format(2, ',', ' ')  #}
                    <td>{{ (item.product.price) / 100 }} {{ 'EUR'|currency_symbol }}</td>
                    <td>{{ item.qty }}</td>
                    <td>{{ item.total / 100 }} {{ 'EUR'|currency_symbol }}</td>
                    <td>
                        <a href="{{ path('cart_delete', {'id': item.product.id}) }}" class="btn btn-danger">
                            <i class="fas fa-trash"></i>
                        </a>
                    </td>
                </tr>
            {% endfor %}
        </tbody>
        <tfoot>
        <tr>
            <td colspan="3">Total: </td>
            <td>{{ total / 100 }} {{ 'EUR'|currency_symbol }}</td>
        </tr>
        </tfoot>
    </table>

{% endblock %}
